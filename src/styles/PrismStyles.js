import { css } from "styled-components"

// PrismJS syntax-highlighting styles. The /pensieve blog section has no
// posts so no element on the site currently uses these selectors. The
// rules were shipping inline on every page (~5 KB) as dead CSS, so the
// file was emptied. If posts return, restore from git history — the
// previous file targeted Night Owl / Material Ocean colors.
const PrismStyles = css``

export default PrismStyles
